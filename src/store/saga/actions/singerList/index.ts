import { createRoutine } from 'redux-saga-routines'
import NAME_SPACE from '../../../../constants/name-space'
const singerListRoutine = createRoutine(`${NAME_SPACE.SINGER_LIST}`) 

export default singerListRoutine