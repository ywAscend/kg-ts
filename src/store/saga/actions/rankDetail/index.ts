import { createRoutine } from 'redux-saga-routines'
import NAME_SPACE from '../../../../constants/name-space'
const rankDetailRoutine = createRoutine(`${NAME_SPACE.RANK_DETAIL}`)

export default rankDetailRoutine