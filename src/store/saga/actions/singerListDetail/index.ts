import { createRoutine } from 'redux-saga-routines'
import NAME_SPACE from '../../../../constants/name-space'

const singerListDetailRoutine = createRoutine(`${NAME_SPACE.SINGER_LIST_Detail}`) 

export default singerListDetailRoutine